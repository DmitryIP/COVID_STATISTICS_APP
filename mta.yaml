ID: COVID_STATISTICS_APP
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: csa_db
   type: hdb
   path: csa_db
   requires:
    - name: hdi_csa_db

resources:
 - name: hdi_csa_db
   parameters:
      config:
         schema: CSA_DB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
